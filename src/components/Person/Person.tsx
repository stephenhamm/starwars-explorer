import React from 'react';

interface Details {
  name: string;
  height: number;
  mass: number;
  hairColor: string;
  skinColor: string;
  gender: string;
  birthYear: number;
}

const Person = (props: Details) => {
  <h1>Person</h1>
}

export default Person;